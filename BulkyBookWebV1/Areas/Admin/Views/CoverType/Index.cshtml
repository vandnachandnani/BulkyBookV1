@model IEnumerable<CoverType>
@{
    ViewData["Title"] = "Index";
}

<div class="container p-3">
    <div class="row pt-4">
        <div class="col-6">
            <h2 class="text-primary">Cover Type List</h2>
        </div>
          <div class="col-6 text-end">
              <a asp-controller="CoverType" asp-action="Create"  asp-area="Admin" class="btn btn-primary">
                  <i class="bi bi-plus-circle"></i>
                  Create New CoverType
              </a>
          </div>
    </div>
</div>
<table class="table table-bordered table-striped" style="width:100%" border="1">
    <thead>
        <tr>
            <th>CoverType Name</th>
            <th></th>
        </tr>
        
    </thead>
    <tbody>
        @foreach(var obj in Model)
        {
            <tr>
                <td width="50%">@obj.Name</td>
                <td>
                    <div class="w-75 btn-group" role="group">
                        <a asp-action="Edit" asp-controller="CoverType" asp-route-id="@obj.Id" class="btn btn-primary mx-2"  asp-area="Admin"><i class="bi bi-pencil-square"></i>Edit</a>
                        <a asp-action="Delete" asp-controller="CoverType" asp-route-id="@obj.Id" class="btn btn-danger mx-2"  asp-area="Admin"><i class="bi bi-trash-fill"></i>Delete</a>
                    </div>
                </td>
            </tr>
        }
    </tbody>
</table>

